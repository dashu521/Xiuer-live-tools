## 执行计划

### 修改内容

#### 文件 1: src/pages/AutoMessage/index.tsx
- 移除 `guardAction` 调用和相关导入
- 直接调用 `startTask('autoSpeak')`

#### 文件 2: src/pages/AutoPopUp/index.tsx  
- 移除 `guardAction` 调用和相关导入
- 直接调用 `onStartTask()`

### 预期结果
- 只有"连接直播中控台"按钮触发鉴权流程
- 其他按钮（自动发言、自动弹窗等）不触发鉴权